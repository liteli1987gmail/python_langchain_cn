
[comment: 请在此处添加一个参考示例 "docs/integrations/arxiv.md"]::
[comment: 使用此模板在 "docs/integrations/" 中创建一个新的 .md 文件]::

# 标题_REPLACE_ME

[comment: 只允许一个标题/H1！]::

>
 
[comment: 描述：读者阅读完这段描述后，应该能决定是否值得尝试/继续阅读，或者]::
[comment: 前往阅读下一个集成文档。]::
[comment: 描述中应包含指向源文件的链接，以供进一步阅读。]::

## 安装和设置

[comment: 安装和设置：所有必要的额外软件包安装和设置（如 Tokens 等）]::

```bash
pip install package_name_REPLACE_ME
```

[comment: 或者此文本：]::
不需要任何特殊的设置。


[comment: 下一个 H2/## 部分是集成模块的名称，如 "LLM"、"文本嵌入模型" 等]::
[comment: 参见 "index.html" 页面中的 "模块"]::
[comment: 每个 H2 部分应包含一个示例链接和导入集成类的 Python 代码]::
[comment: 以下是几个示例部分。删除所有不必要的部分。添加所有未提供的必要部分。]::

## LLM

查看一个[使用示例](/docs/modules/model_io/models/llms/integrations/INCLUDE_REAL_NAME.html)。

```python
from langchain.llms import integration_class_REPLACE_ME
```


## 文本嵌入模型

查看一个[使用示例](/docs/modules/data_connection/text_embedding/integrations/INCLUDE_REAL_NAME.html)

```python
from langchain.embeddings import integration_class_REPLACE_ME
```


## 聊天模型

查看一个[使用示例](/docs/modules/model_io/models/chat/integrations/INCLUDE_REAL_NAME.html)

```python
from langchain.chat_models import integration_class_REPLACE_ME
```

## 文档加载器

查看一个[使用示例](/docs/modules/data_connection/document_loaders/integrations/INCLUDE_REAL_NAME.html)。

```python
from langchain.document_loaders import integration_class_REPLACE_ME
```
