import CodeBlock from "@theme/CodeBlock";

import AnalyzeDocumentExample from "!!raw-loader!@examples/chains/analyze_document_chain_summarize.ts";


# `AnalyzeDocumentChain`

您可以使用`AnalyzeDocumentChain`,它接受单个文本作为输入并对其进行操作。
这个链条负责拆分文本，然后将其传递给 `MapReduceDocumentsChain` 生成摘要。


<CodeBlock language="typescript">{AnalyzeDocumentExample}</CodeBlock>

