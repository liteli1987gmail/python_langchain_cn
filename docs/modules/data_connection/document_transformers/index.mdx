---
sidebar_position: 1
---
# 文档转换器

一旦您加载了文档，通常会希望对其进行转换，以更好地适应您的应用程序。最简单的示例
是您可能希望将长文档拆分为适合模型上下文窗口的较小块。LangChain
具有许多内置的文档转换器，使得拆分、合并、过滤和其他操作文档变得非常容易。

## 文本拆分器

当您想处理长段文本时，有必要将文本拆分成块。
尽管听起来很简单，但这里存在许多潜在的复杂性。理想情况下，您希望保持语义相关的文本片段在一起。"语义相关"的含义可能取决于文本的类型。
本笔记本展示了几种实现这一目标的方式。

从较高层面上看，文本拆分器的工作方式如下:

1. 将文本拆分成小的、语义上有意义的块（通常是句子）。
2. 将这些小块组合成一个较大的块，直到达到一定的大小（通过某个函数来衡量）。
3. 一旦达到该大小，将该块作为独立的文本片段，然后开始创建一个具有一定重叠的新文本块（以保持块之间的上下文）。

这意味着您可以沿着两个不同的轴自定义文本拆分器:

1. 如何进行文本拆分
2. 如何测量块的大小

## 开始使用文本拆分器

import GetStarted from "@snippets/modules/data_connection/document_transformers/get_started.mdx"

<GetStarted/>
