# 会话缓冲窗口记忆 ( Conversation buffer window memory )

`ConversationBufferWindowMemory` 会随着时间记录会话的交互列表。它只使用最后 K 个交互。这对于保持最近交互的滑动窗口很有用，以避免缓冲区过大

让我们首先探索这种类型记忆的基本功能。

import Example from "@snippets/modules/memory/how_to/buffer_window.mdx"

<Example/>
