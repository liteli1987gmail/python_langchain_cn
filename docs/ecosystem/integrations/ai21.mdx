# AI21 实验室

本页面介绍如何在 LangChain 中使用 AI21 生态系统。
它分为两个部分：安装和设置，然后是对特定 AI21 包装器的引用。

## 安装和设置
- 获取 AI21 的 API 密钥，并将其设置为环境变量（`AI21_API_KEY`）

## 包装器

### LLM

存在一个 AI21 LLM 包装器，您可以使用以下方式访问
```python
from langchain.llms import AI21
```
