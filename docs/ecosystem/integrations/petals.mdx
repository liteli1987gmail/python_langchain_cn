# Petals（花瓣）

本页面介绍如何在 LangChain 中使用 Petals 生态系统。
它分为两个部分：安装和设置，以及特定 Petals 封装的参考资料。

## 安装和设置
- 使用 `pip install petals` 进行安装
- 获取一个 Hugging Face API 密钥，并将其设置为环境变量（`HUGGINGFACE_API_KEY`）

## 封装

### LLM

存在一个 Petals LLM 封装，可以通过以下方式访问
```python
from langchain.llms import Petals
```
