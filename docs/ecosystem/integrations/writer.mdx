# 写手

本页介绍如何在LangChain中使用写手生态系统。
它分为两个部分：安装和设置，以及特定写手包的参考。

## 安装和设置
- 获取一个写手API密钥，并将其设置为环境变量（`WRITER_API_KEY`）

## 包装器

### LLM

存在一个写手LLM包装器，可以使用以下方式访问
```python
from langchain.llms import Writer
```