# Argilla

![Argilla - LLM 的开源数据平台](https://argilla.io/og.png)

> [Argilla](https://argilla.io/) 是一个为 LLM 提供开源数据整理平台。
> 使用 Argilla，任何人都可以通过更快的数据整理构建强大的语言模型
> 使用人工和机器反馈。我们为 MLOps 周期的每个步骤提供支持
> 从数据标注到模型监控。

## 安装和设置

首先，您需要按照以下方式安装 `argilla` Python 包:

```bash
pip install argilla --upgrade
```

如果您已经运行了 Argilla 服务器，那么您可以开始了；但如果
没有的话，请按照以下步骤安装它。

如果您没有，可以参考 [Argilla - 🚀 快速入门](https://docs.argilla.io/en/latest/getting_started/quickstart.html#Running-Argilla-Quickstart) 在 HuggingFace Spaces、本地或服务器上部署 Argilla。

## 跟踪

请参阅 [`ArgillaCallbackHandler` 的使用示例](/docs/modules/callbacks/integrations/argilla.html)。

```python
from langchain.callbacks import ArgillaCallbackHandler
```

